spring:
  datasource:
    url: 'jdbc:h2:mem:my-way_db'
    username: 'username'
    password: 'password'
    driver-class-name: org.h2.Driver
  flyway:
    url: 'jdbc:h2:mem:my-way_db'
    user: 'username'
    password: 'password'
  h2:
    console:
      enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none